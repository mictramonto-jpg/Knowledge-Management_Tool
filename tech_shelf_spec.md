# 技術の棚 Excel 実装仕様書

> 実装方針: **D（ローカル .xlsx 生成 → 後日 SharePoint アップロードで同時編集化）**
> 同時編集に耐える設計（Excel テーブル化／マクロなし／シート保護）を最初から組み込む。

---

## 0. 実装スタック

| 項目 | 選択 |
|------|------|
| 形式 | `.xlsx`（マクロなし） |
| ライブラリ | `openpyxl` |
| Python | 3.10+ |
| 出力先 | `output/技術の棚.xlsx`（CLI で `--output` で上書き可能にする） |
| フォント | Meiryo UI（無ければ Arial フォールバック） |
| 想定 Excel バージョン | Microsoft 365（FILTER/SORT/CHOOSECOLS/XLOOKUP などの動的配列関数を使用） |

### SharePoint 同時編集対応のための原則

- **Excel テーブル化**: 「技術の棚」「実施事項」シートのデータ範囲を `ListObject`（Excel テーブル）にする。行追加で書式・数式・データバリデーションが自動伝搬し、複数人の同時末尾追加で衝突しにくい。
- **マクロは使わない**（`.xlsx` を維持）→ ブラウザ版 Excel Online で開ける。
- **ActiveX・旧「ブックの共有」機能は使わない**。
- **シート保護**は警告レベル（パスワードなし）。「会議ビュー」「マスタ」シートを誤編集から守る。
- **名前付き範囲**でデータバリデーションを参照（マスタ更新が即時反映）。

---

## 1. 概要

### 1.1 目的
社内の品質ノウハウを蓄積するDB「技術の棚」を、Excelで構造化して管理する。
社員全体で更新可能な触りやすさを維持しつつ、品質会議で活用できる構造化を実現する。

### 1.2 全体構成
4シート構成。シート間は案件IDで連携する。

```
技術の棚.xlsx
├─ シート1: 技術の棚（メインの案件マスター・テーブル化）
├─ シート2: 実施事項（案件に紐づくタスク管理・テーブル化）
├─ シート3: 会議ビュー（閲覧専用の集計ビュー・保護）
└─ シート4: マスタ（プルダウン選択肢の管理・保護）
```

---

## 2. シート1：技術の棚（メイン）

### 2.1 役割
1行 = 1案件。欠点事例ごとの基本情報・原因・対策アプローチ・効果確認・標準化反映状況を一元管理する。

### 2.2 列定義（39列：A〜AM）

| 列 | 列名 | データ型 | 入力方式 | 説明 |
|----|------|----------|----------|------|
| A | 案件ID | 文字列 | 自動採番（数式） | `T2026-001`形式。年で区切り、001から連番 |
| B | 日付 | 日付 | 手入力 | トピックをリストアップした日 |
| C | 欠点名 | 文字列 | プルダウン（マスタ参照、追記可能） | 織物に入った欠点名 |
| D | 品番 | 文字列 | 手入力 | 実際にその欠点が入っていた品番 |
| E | 工場 | 文字列 | プルダウン（マスタ参照） | 製造していた工場 |
| F | 責任者 | 文字列 | プルダウン（マスタ参照） | 対策責任者 |
| G | 生技 | 文字列 | プルダウン（マスタ参照） | 生産技術担当者 |
| H | テキ技 | 文字列 | プルダウン（マスタ参照） | テキスタイル技術課（品証）担当者 |
| I | 客先 | 文字列 | 手入力 | 品質に関して客先とすり合わせを行う案件で記入 |
| J | 発生状況 | 文字列（複数行可） | 手入力 | 欠点の発生状況 |
| K | 推定要因 | 文字列（複数行可） | 手入力 | 欠点が発生した推定要因 |
| L | 処置 | 文字列（複数行可） | 手入力 | 発生時に行った応急処置（一時的対応） |
| M | 条件 | 文字列 | プルダウン（〇/△/×/空白） | 製織条件でアプローチするか |
| N | 設計 | 文字列 | プルダウン（〇/△/×/空白） | 設計でアプローチするか |
| O | 原糸メーカー | 文字列 | プルダウン（〇/△/×/空白） | 原糸メーカー側でアプローチするか |
| P | 宿題 | 文字列（複数行可） | 手入力 | 次にやることのメモ |
| Q | 原理原則 | 文字列（複数行可） | 手入力 | 発生メカニズムと解決/未解決の理屈 |
| R〜AA | N1〜N10 | 文字列 | プルダウン（〇/△/×/空白） | 1〜10回目の試験結果 |
| AB | 評価 | 文字列 | プルダウン（〇/△/×/空白） | 総合評価 |
| AC | 標準作業表 | 文字列 | プルダウン（〇/×/対象外/空白） | 標準作業表への反映 |
| AD | ワンポ | 文字列 | プルダウン（〇/×/対象外/空白） | ワンポイントレッスンへの反映 |
| AE | 製織条件表 | 文字列 | プルダウン（〇/×/対象外/空白） | 製織条件表への反映 |
| AF | 準備条件表 | 文字列 | プルダウン（〇/×/対象外/空白） | 準備条件表への反映 |
| AG | 検査基準 | 文字列 | プルダウン（〇/×/対象外/空白） | 検査基準への反映 |
| AH | 検査コメント | 文字列 | プルダウン（〇/×/対象外/空白） | 検査コメントへの反映 |
| AI | 限度見本 | 文字列 | プルダウン（〇/×/対象外/空白） | 限度見本への反映 |
| AJ | 案件ステータス | 文字列 | プルダウン | 案件全体のステータス（後述） |
| AK | 実施事項件数 | 数値 | 自動（数式） | この案件に紐づく実施事項の総数 |
| AL | 未完了数 | 数値 | 自動（数式） | 未完了の実施事項数 |
| AM | 最新の動き | 文字列 | 自動（数式） | 直近の実施事項を1行表示 |

### 2.3 Excel テーブル化

データ範囲 `A1:AM1001`（ヘッダー1行 + 1000データ行）を `ListObject` として登録する。

```python
from openpyxl.worksheet.table import Table, TableStyleInfo
table = Table(displayName="T_TechShelf", ref="A1:AM1001")
style = TableStyleInfo(name="TableStyleLight1", showRowStripes=True)
table.tableStyleInfo = style
ws.add_table(table)
```

テーブル化により：
- 行追加で書式・数式・データバリデーションが自動伝搬
- 末尾への同時行追加で衝突しにくい（SharePoint 同時編集の事故防止）
- 構造化参照（`T_TechShelf[案件ID]`）が使える

### 2.4 案件ID自動採番（A列）

A2 以降に以下の数式（テーブル化されているので、行追加時も自動で複製される）：

```excel
=IF([@日付]="","",CONCATENATE("T",TEXT(YEAR([@日付]),"0000"),"-",TEXT(COUNTIF(INDEX([日付],1):[@日付],">="&DATE(YEAR([@日付]),1,1)),"000")))
```

- 構造化参照 `[@日付]` を使用
- 年が変わると連番が001にリセット

### 2.5 N5〜N10列の初期非表示

V〜AA列をデフォルトで非表示。

```python
for col in ["V", "W", "X", "Y", "Z", "AA"]:
    ws.column_dimensions[col].hidden = True
```

### 2.6 案件ステータス（AJ列）の選択肢
- 発生 / 原因分析中 / 対策試験中 / 効果確認済 / 標準化中 / 完了 / 保留 / 中止

### 2.7 自動集計列の数式

#### AK列：実施事項件数
```excel
=IF([@案件ID]="","",COUNTIF(T_Tasks[案件ID],[@案件ID]))
```

#### AL列：未完了数
```excel
=IF([@案件ID]="","",COUNTIFS(T_Tasks[案件ID],[@案件ID],T_Tasks[ステータス],"<>完了",T_Tasks[ステータス],"<>中止"))
```

#### AM列：最新の動き（Excel 365 動的配列）
```excel
=IFERROR(INDEX(T_Tasks[実施項目],MATCH(1,(T_Tasks[案件ID]=[@案件ID])*(T_Tasks[登録日]=MAXIFS(T_Tasks[登録日],T_Tasks[案件ID],[@案件ID])),0)),"")
```

Excel 365 では `Ctrl+Shift+Enter` 不要。openpyxl では数式文字列として設定すれば動作する。

### 2.8 書式設定

- 1行目（ヘッダー行）: 太字、背景色 薄い青（RGB: 217, 226, 243）、中央揃え
- 折り返して全体を表示: J/K/L/P/Q/AM 列
- フリーズペイン: B2 セル
- オートフィルタ: テーブル化により自動付与
- 行高: 自動調整、最低40
- 列幅:
  - A: 13 / B: 12 / C: 15 / D: 14
  - E〜H: 10 / I: 15
  - J/K/L: 30
  - M/N/O: 8
  - P/Q: 30
  - R〜AB: 6
  - AC〜AI: 10
  - AJ: 12 / AK/AL: 8 / AM: 25

### 2.9 条件付き書式

- 案件ステータス（AJ列）が「完了」の行: 行全体を薄いグレー背景
- 未完了数（AL列）が0より大きい: AL列を薄い赤背景
- 評価（AB列）が「×」: 該当セルを赤文字

---

## 3. シート2：実施事項

### 3.1 役割
1行 = 1タスク。案件IDで親シート（技術の棚）と紐づく。対策検討の中で実施するタスクを時系列で蓄積する。

### 3.2 列定義（13列：A〜M）

| 列 | 列名 | データ型 | 入力方式 | 説明 |
|----|------|----------|----------|------|
| A | タスクID | 文字列 | 自動採番（数式） | `案件ID-項番`形式 |
| B | 案件ID | 文字列 | プルダウン（技術の棚A列を参照） | 紐づく案件のID |
| C | 項番 | 数値 | 自動（数式） | 同じ案件内での連番 |
| D | 実施項目 | 文字列（複数行可） | 手入力 | タスクの内容 |
| E | 担当 | 文字列 | プルダウン（マスタ参照） | タスク担当者 |
| F | 着手予定日 | 日付 | 手入力 | |
| G | 期限 | 日付 | 手入力 | 完了予定日 |
| H | 実着手日 | 日付 | 手入力 | 実際に着手した日 |
| I | 実完了日 | 日付 | 手入力 | 実際に完了した日 |
| J | 進捗% | 数値 | プルダウン（0/25/50/75/100） | 進捗パーセント |
| K | ステータス | 文字列 | プルダウン（6種） | 未着手/調査中/試験中/完了/保留/中止 |
| L | 結果メモ | 文字列（複数行可） | 手入力 | タスクの結果や経緯 |
| M | 登録日 | 日付 | 手入力 | このタスクを登録した日 |

### 3.3 Excel テーブル化

```python
table = Table(displayName="T_Tasks", ref="A1:M1001")
```

### 3.4 タスクID自動採番（A列）
```excel
=IF([@案件ID]="","",CONCATENATE([@案件ID],"-",TEXT([@項番],"00")))
```

### 3.5 項番自動採番（C列）
```excel
=IF([@案件ID]="","",COUNTIF(INDEX([案件ID],1):[@案件ID],[@案件ID]))
```

### 3.6 書式設定

- 1行目（ヘッダー行）: 太字、背景色 薄い緑（RGB: 226, 239, 218）、中央揃え
- 折り返して全体を表示: D/L 列
- フリーズペイン: B2 セル
- 列幅:
  - A: 16 / B: 13 / C: 6 / D: 35
  - E: 10 / F〜I: 12
  - J: 8 / K: 10 / L: 30 / M: 12

### 3.7 条件付き書式

- ステータスが「完了」: 行全体を薄いグレー背景
- ステータスが「中止」: 行全体を薄いグレー背景＋取り消し線
- 期限（G列）が今日より前 かつ ステータスが「完了」「中止」以外: 期限セルを赤背景＋白文字
- 期限（G列）が7日以内 かつ ステータスが「完了」「中止」以外: 期限セルを黄色背景

---

## 4. シート3：会議ビュー（閲覧専用）

### 4.1 役割
品質会議で使う閲覧専用の集計ビュー。シート保護をかける。

### 4.2 セクション構成

各セクションは Excel 365 の動的配列関数（`FILTER` / `CHOOSECOLS` / `SORT`）で実装。

#### セクション1：今週のタスク（A1〜）
ヘッダー: 「今週のタスク（期限が今日〜7日以内）」
列: タスクID / 案件ID / 実施項目 / 担当 / 期限 / ステータス

```excel
=IFERROR(CHOOSECOLS(FILTER(実施事項!A2:M1001,(実施事項!G2:G1001>=TODAY())*(実施事項!G2:G1001<=TODAY()+7)*(実施事項!K2:K1001<>"完了")*(実施事項!K2:K1001<>"中止"),"該当なし"),1,2,4,5,7,11),"該当なし")
```

#### セクション2：遅延タスク
ヘッダー: 「遅延タスク（期限超過）」

```excel
=IFERROR(CHOOSECOLS(FILTER(実施事項!A2:M1001,(実施事項!G2:G1001<TODAY())*(実施事項!G2:G1001<>"")*(実施事項!K2:K1001<>"完了")*(実施事項!K2:K1001<>"中止"),"該当なし"),1,2,4,5,7,11),"該当なし")
```
行全体を赤背景。

#### セクション3：未完了タスク（担当者別件数）
ヘッダー: 「未完了タスク（担当者別件数）」

```excel
=LET(
  data, FILTER(実施事項!E2:E1001,(実施事項!E2:E1001<>"")*(実施事項!K2:K1001<>"完了")*(実施事項!K2:K1001<>"中止"),""),
  uniq, UNIQUE(data),
  HSTACK(uniq, BYROW(uniq, LAMBDA(r, COUNTIF(data, r))))
)
```

#### セクション4：効果確認待ち案件
ヘッダー: 「効果確認待ち案件（評価が未入力）」
列: 案件ID / 欠点名 / 品番 / 工場 / 責任者 / N1 / N2 / N3 / N4 / 評価

```excel
=IFERROR(CHOOSECOLS(FILTER(技術の棚!A2:AB1001,(技術の棚!AB2:AB1001="")*(技術の棚!A2:A1001<>""),"該当なし"),1,3,4,5,6,18,19,20,21,28),"該当なし")
```

#### セクション5：ガントチャート（簡易）
- 縦軸: タスクID + 実施項目 + 担当
- 横軸: 月（現在月から前後3ヶ月、合計7ヶ月）
- セル内: 着手予定日〜期限の期間に該当する月に塗りつぶし
- 実装: 条件付き書式で `MONTH(着手予定日)<=月 AND MONTH(期限)>=月` の条件で塗りつぶし
- 初期版では簡易ガント、将来拡張可能

### 4.3 書式設定

- セクション間に空行2行
- セクションヘッダー: 太字、背景色 紺（RGB: 31, 73, 125）、白文字、フォントサイズ12
- 列ヘッダー: 太字、背景色 薄いグレー（RGB: 217, 217, 217）

### 4.4 シート保護
シート全体を保護。`sheet.protection.sheet = True` でパスワードなしで編集制限。
編集を試みた際に警告が出る程度の保護で十分（読み取り専用感を出す）。

```python
ws.protection.sheet = True
ws.protection.password = None
```

---

## 5. シート4：マスタ

### 5.1 役割
プルダウンの選択肢を一元管理する。メンテナンスはこのシートだけで完結する。

### 5.2 構成

| 列 | カテゴリ | 初期値 |
|----|----------|--------|
| A | 工場 | 第一/第二/第三/第四/JET/本社/黒氏 |
| B | 担当者 | 担当者1〜担当者10 |
| C | 欠点名 | ソーコ汚れ/片引け/キワ付き/緯弛み/シワ当り/耳不良 |
| D | 評価記号 | 〇/△/×/空白 |
| E | 標準化記号 | 〇/×/対象外/空白 |
| F | 案件ステータス | 発生/原因分析中/対策試験中/効果確認済/標準化中/完了/保留/中止 |
| G | タスクステータス | 未着手/調査中/試験中/完了/保留/中止 |
| H | 進捗% | 0/25/50/75/100 |

### 5.3 名前付き範囲

将来の追加に備えて A2:A100 のように余裕を持たせる。

```python
from openpyxl.workbook.defined_name import DefinedName

defined_names = {
    "工場リスト":         "マスタ!$A$2:$A$100",
    "担当者リスト":       "マスタ!$B$2:$B$100",
    "欠点名リスト":       "マスタ!$C$2:$C$100",
    "評価記号リスト":     "マスタ!$D$2:$D$5",
    "標準化記号リスト":   "マスタ!$E$2:$E$5",
    "案件ステータスリスト": "マスタ!$F$2:$F$100",
    "タスクステータスリスト": "マスタ!$G$2:$G$100",
    "進捗パーセントリスト": "マスタ!$H$2:$H$6",
}
for name, ref in defined_names.items():
    wb.defined_names[name] = DefinedName(name, attr_text=ref)
```

### 5.4 書式設定
- ヘッダー行: 太字、背景色 薄い黄色（RGB: 255, 242, 204）

### 5.5 シート保護
シート保護をかける（マスタは管理者のみ編集する想定）。
`sheet.protection.sheet = True` でパスワードなし保護。

---

## 6. データバリデーション設定

### 6.1 シート1：技術の棚

| 列 | 参照名前 |
|----|----------|
| C（欠点名） | 欠点名リスト |
| E（工場） | 工場リスト |
| F、G、H（責任者・生技・テキ技） | 担当者リスト |
| M、N、O（条件・設計・原糸メーカー） | 評価記号リスト |
| R〜AA（N1〜N10） | 評価記号リスト |
| AB（評価） | 評価記号リスト |
| AC〜AI（標準化8列） | 標準化記号リスト |
| AJ（案件ステータス） | 案件ステータスリスト |

### 6.2 シート2：実施事項

| 列 | 参照名前 |
|----|----------|
| B（案件ID） | `=技術の棚!$A$2:$A$1001` |
| E（担当） | 担当者リスト |
| J（進捗%） | 進捗パーセントリスト |
| K（ステータス） | タスクステータスリスト |

### 6.3 設定範囲

各列の A2:A1001 など1000行分に設定する。テーブル化されているので、それを超える行追加時も自動でコピーされる。

```python
from openpyxl.worksheet.datavalidation import DataValidation

dv = DataValidation(type="list", formula1="=工場リスト", allow_blank=True)
dv.add("E2:E1001")
ws.add_data_validation(dv)
```

---

## 7. 初期データ

### 7.1 移行データ
既存の `技術の棚.xlsx` の7件をそのまま移行する想定。本実装ではまず空のテンプレートを生成し、移行は別途対応する。

### 7.2 移行時の注意
既存データのP列に詰まっている箇条書きは、人間の判断で以下のいずれかに振り分ける：
- 「次にやること」 → P列「宿題」
- 「発生の理屈・解決の理屈」 → Q列「原理原則」
- 「個別タスク」 → 実施事項シートに分割登録

---

## 8. 実装手順

1. `requirements.txt` に `openpyxl` を記載
2. `tech_shelf_builder.py` を作成
3. CLI 引数: `--output PATH`（デフォルト `output/技術の棚.xlsx`）
4. openpyxl で空の Workbook を作成、4シートを生成
5. シート4（マスタ）にプルダウン選択肢を投入＋名前付き範囲を定義
6. シート1（技術の棚）の構造とヘッダー・列幅・書式・テーブル化・数式を設定
7. シート2（実施事項）の構造とヘッダー・列幅・書式・テーブル化・数式を設定
8. シート3（会議ビュー）の構造とヘッダー・書式・数式を設定
9. データバリデーションを各シートに設定
10. 条件付き書式を設定
11. シート3・シート4に保護をかける
12. 保存

---

## 9. 動作確認チェックリスト

### Excel デスクトップ版
- [ ] 技術の棚シートで日付を入れると、案件IDが自動採番される
- [ ] 工場・担当者などのプルダウンが機能する
- [ ] N5〜N10列がデフォルトで非表示
- [ ] 実施事項シートで案件IDがプルダウンから選べる
- [ ] 項番が同じ案件内で自動連番される
- [ ] タスクIDが「案件ID-項番」形式で自動生成される
- [ ] 技術の棚の実施事項件数・未完了数が、実施事項シートのデータに連動する
- [ ] 期限超過タスクが赤背景になる
- [ ] 会議ビューの各セクションが動的に集計される
- [ ] 会議ビュー・マスタが保護されている
- [ ] マスタを編集するとプルダウン選択肢が変わる

### SharePoint アップロード後（同時編集）
- [ ] ブラウザ版 Excel Online で開ける（マクロエラーが出ない）
- [ ] 2人で別のセル/別の行を同時編集しても衝突しない
- [ ] テーブル末尾への行追加で書式・数式が自動継承される

---

## 10. 将来の拡張ポイント

- 効果確認（N1〜N10）が10回を超える場合 → 別シート化を検討
- 実施事項の階層化（マイルストーン管理）が必要になった場合 → 子シート追加
- データが数百件を超える場合 → SharePoint リスト化または業務管理アプリへの移行を検討
- SharePoint アップロード後 → Power Automate で期限通知、Teams 連携を追加
- 案件にファイル添付したい場合 → ハイパーリンク列を追加

---

## 11. 新セッションでの実装依頼テンプレート

新セッションでは以下のように依頼すればよい：

```
このリポジトリの tech_shelf_spec.md に従って「技術の棚」Excel管理ツールを実装してください。

実装方針:
- ローカル .xlsx 生成（openpyxl）
- 後日 SharePoint にアップロードして同時編集する想定
- マクロなし、Excel テーブル化、シート保護を組み込み済み

手順:
1. tech_shelf_spec.md を読み込んで仕様を把握
2. requirements.txt（openpyxl のみ）を作成
3. tech_shelf_builder.py を作成
4. 出力先: output/技術の棚.xlsx（--output で上書き可能）
5. シート4（マスタ）→ シート1（技術の棚）→ シート2（実施事項）→ シート3（会議ビュー）の順に組む
6. 生成後、LibreOffice で開いてエラーが出ないことを確認

Python は venv で隔離。依存は openpyxl のみ。
```
